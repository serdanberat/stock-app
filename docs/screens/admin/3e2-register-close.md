# 3.E.2 — Cash Register Close + Z Report

> **Status:** Locked (Phase 3.E)
> **Routes:**
> - `/cash-register/close` — Close form
> - `/cash-register/sessions/{id}/z-report` — View/reprint
> - `/cash-register/sessions` — History list

## Purpose

End-of-shift / day session close. Counts physical cash, computes variance, generates immutable Z report PDF.

## Aggregate ownership (explicit)

- **Writes** CashRegisterSession aggregate (status transition)
- Indirectly via outbox event consumer:
  - `cash_movement` CLOSING_DEPOSIT (if cash removed from drawer)
  - `cash_movement` CORRECTION (if variance, with reason captured)
  - Z report document generated by document worker (Phase 6.F)

## Z report immutability

Z report is a snapshot bound to the closed session. Once generated:
- PDF stored via DocumentStorage
- Source data (movement totals, tender breakdown, variances) captured in `z_reports` table
- **Reprint renders the SAME stored payload, NOT regenerates**
- Same payload always produces identical PDF (deterministic)

Rationale: tax/legal audit. Z reports are accounting artifacts; immutability is non-negotiable.

### Byte-determinism invariant (CRITICAL)

For "same payload → identical PDF bytes" claim to hold, the PDF generator MUST NOT embed any dynamic content not derived from `snapshot_payload`:

- ✗ NO `now()` timestamps at render time
- ✗ NO render request IDs
- ✗ NO server hostname / instance ID
- ✗ NO PDF generation library version string (use stable library, pin version)
- ✗ NO compression timestamp in PDF metadata

All timestamps shown in PDF MUST come from `snapshot_payload`:
- "Closed at:" → from `payload.session_closed_at`
- "Generated for date:" → from `payload.business_date`
- Any other temporal reference → must be a payload field

**Test requirement (Phase 7)**: byte-identical reprint test in CI:
```
1. Generate PDF from payload P → SHA256(pdf_v1)
2. Generate PDF from payload P again → SHA256(pdf_v2)
3. Assert pdf_v1 == pdf_v2 (byte-equal)
```

PDF library configuration: deterministic mode (e.g. Gotenberg 8 with `--no-pdf-version-fingerprint`, fixed font subset, no creation date metadata).

## Reads

- `GET /cash-register/sessions/{id}/summary`
  - Returns expected closing state:
    - `opening_float`
    - `cash_in_total` (SALE_CASH_IN + customer payments + etc.)
    - `cash_out_total` (SALE_CHANGE_OUT + refunds + payments out)
    - `expected_cash = opening + cash_in - cash_out`
    - `tender_breakdown` (sales by CASH/CARD/CUSTOMER_ACCOUNT)
    - `sale_count`, `refund_count`

## Writes

- `POST /cash-register/sessions/{id}/close`
  - Body:
    ```
    {
      counted_cash_amount,
      remaining_float_amount?,     // cash to leave in drawer for next session
                                   // System computes: cash_removed = expected_cash - remaining_float
      variance_reason?,            // required if variance > tolerance
      variance_note?
    }
    ```
  - Idempotency-Key required
  - Atomically:
    - status = CLOSED
    - If variance: `cash_movement` CORRECTION
    - If remaining_float < expected_cash: `cash_movement` CLOSING_DEPOSIT for the removed delta
    - Z report row created (status PENDING_GENERATION)
    - Outbox event ZReportRequested triggers document worker

## Variance handling

```
variance = counted_cash_amount - expected_cash
```

`|variance| <= tenant.cash_variance_tolerance` (default ₺5):
- No reason required; variance recorded but treated as routine

`|variance| > tolerance`:
- Reason mandatory from closed set:
  - `SHORT_CASHIER_ERROR` — Kasiyer hatası (eksik)
  - `SHORT_UNKNOWN` — Eksik, sebep belirsiz
  - `OVER_CASHIER_ERROR` — Kasiyer hatası (fazla)
  - `OVER_UNKNOWN` — Fazla, sebep belirsiz
  - `MISCOUNT` — Yanlış sayım (recount yapıldı)
  - `SUSPECTED_THEFT` — Hırsızlık şüphesi (manager investigates)
- Note mandatory if SUSPECTED_THEFT

Large variance threshold (tenant config, default ₺100):
- Requires manager PIN override

## Optimistic UI

NO. Critical financial state.

## Locking

Pessimistic FOR UPDATE on cash_register_session row + cash_movements related during close TX.

## Idempotency

Required (X-Idempotency-Key).

## Keyboard flow

| Key | Action |
|---|---|
| Tab | counted_cash → remaining_float → variance fields → close |
| `Ctrl+S` | Close session |
| `Esc` | Cancel (returns to POS) |

## Speed budget

| Action | p95 target |
|---|---|
| Summary load | < 400ms |
| Close submit | < 800ms |
| Z report ready | < 5s (async; document worker) |

## Permissions

| Permission | Default |
|---|---|
| `cashregister.close` | CASHIER+ (own session) |
| `cashregister.close_large_variance` | STORE_MANAGER+ (above threshold) |
| `cashregister.view_z_reports` | ACCOUNTANT+, STORE_MANAGER+ |
| `cashregister.reprint_z_report` | ACCOUNTANT+, STORE_MANAGER+ |

## Layout

```
┌─ Kasayı Kapat ────────────────────────────────────────────────────┐
│                                                                    │
│  Beyoğlu - Kasa 1   Oturum: #4521                                  │
│  Açılış: 16/05 09:00   Süre: 8s 24dk                              │
│                                                                    │
│  ┌─ Beklenen Durum ────────────────────────────────────────┐    │
│  │ Açılış nakit:           ₺   500,00                         │    │
│  │ Nakit satış:            ₺ 3.245,00                         │    │
│  │ Para üstü çıkış:        ₺   120,00                         │    │
│  │ Müşteri tahsilatı:      ₺   400,00                         │    │
│  │ Nakit iade:             ₺   150,00                         │    │
│  │ ──────────────────────────                                 │    │
│  │ Beklenen nakit:         ₺ 3.875,00                         │    │
│  │                                                            │    │
│  │ Tender breakdown:                                          │    │
│  │   Kart:                 ₺ 2.130,00 (38 işlem)             │    │
│  │   Müşteri hesabı:       ₺   870,00 (12 işlem)             │    │
│  │   Toplam satış:         ₺ 6.245,00 (78 işlem)             │    │
│  │   İade:                 ₺   180,00 (3 işlem)              │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                    │
│  Fiili nakit sayım: [₺ 3.872,00]                                   │
│  Fark:              -₺ 3,00 (tolerans içinde)                      │
│                                                                    │
│  Kasada bırakılacak nakit:                                         │
│  [₺ 372,00]                                                        │
│  Çıkacak nakit:     ₺ 3.500,00  (sistem hesaplar)                  │
│                                                                    │
│  Not: [_______________________________]                            │
│                                                                    │
│  [Esc İptal]                              [Kapat (Ctrl+S)]         │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Variance section (when exceeds tolerance)

```
┌─ Fark Tespit Edildi ─────────────────────────────────────────────┐
│                                                                    │
│  Beklenen: ₺ 3.875,00                                              │
│  Sayılan:  ₺ 3.750,00                                              │
│  Eksik:    ₺   125,00                                              │
│                                                                    │
│  Sebep (zorunlu):                                                  │
│  [Kasiyer Hatası - Eksik ▾]                                        │
│                                                                    │
│  Açıklama:                                                         │
│  [_______________________________________________]                 │
│                                                                    │
│  ₺125 > ₺100 olduğundan yönetici onayı gerekli:                   │
│  Yönetici PIN: [______]                                            │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Edge cases

| # | Scenario | Behavior |
|---|---|---|
| 1 | Mid-close network failure | Idempotency-Key prevents partial close |
| 2 | Session has uncompleted Sale in AWAITING_PAYMENT | Block close: "1 satış ödeme bekliyor. Önce tamamla veya iptal et" |
| 3 | Z report generation fails | Session still CLOSED (atomic core); document worker retries; manager dashboard alert "Z report failed; manual reprint needed" |
| 4 | Reprint Z report after correction adjustment posted | Z report snapshot frozen; correction shows in NEXT session's opening calculations; reprint shows original variance |

## Audit events

- `cash_session_closed`
- `cash_session_closed_with_variance` (with amount + reason)
- `cash_session_closed_large_variance_override` (manager intervention)
- `z_report_generated`
- `z_report_reprinted` (with actor + timestamp)

## Implementation notes

- Summary load uses materialized aggregate query on cash_movements
- Z report PDF deterministic: same payload → same PDF bytes (see Byte-determinism invariant above)
- Reprint renders stored snapshot; does NOT recompute
- `z_reports` table schema:
  - id, tenant_id, session_id, snapshot_payload (JSONB immutable), pdf_storage_key, generated_at
- **Cash close mental model**: cashier enters "kasada bırakılacak nakit" (what stays in drawer for next session). System computes `cash_removed = expected_cash - remaining_float_amount`. CLOSING_DEPOSIT cash_movement created for the removed delta. This matches cashier's natural thinking ("ne bırakacağım?") rather than the ambiguous "what am I taking out?" which could mean safe deposit, bank transfer, or next-day float.
